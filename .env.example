# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

DATABASE_URL="postgresql://user:password@localhost:5432/secure_bridge_connect?schema=public"

# App
VITE_APP_TITLE=Secure Bridge Connect
VITE_API_URL=http://localhost:3000/api

# Outras variáveis de ambiente
NODE_ENV=development
NEXT_PUBLIC_API_URL="http://localhost:3000/api"
CNPJ_API_KEY="your_api_key_here"

# Authentication
JWT_SECRET=your-jwt-secret
JWT_EXPIRES_IN=7d

# Email
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password

# Storage
STORAGE_TYPE=local
STORAGE_PATH=./uploads

# Logging
LOG_LEVEL=info
LOG_FORMAT=combined

# Instruções:
# 1. Copie este arquivo para .env
# 2. Substitua os valores das variáveis pelos valores reais
# 3. Nunca comite o arquivo .env no repositório 